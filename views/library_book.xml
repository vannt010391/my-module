<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id='library_book_action' model='ir.actions.act_window'>
        <field name="name">Library Books</field>
        <field name="res_model">library.book</field>
        <field name="view_mode">tree,form</field>
    </record>

    <menuitem name="My Library" id="library_base_menu" />
    <menuitem name="Books" id="library_book_menu" parent="library_base_menu" action="library_book_action"/>

    <!-- Form View -->
    <record id="library_book_view_form" model="ir.ui.view">
        <field name="name">Library Book Form</field>
        <field name="model">library.book</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="book_rent" string="Rent this book" type="object" class="btn-primary"/>
                    <!-- <button name="return_all_books" string="Return all book" type="object" class="btn-primary"/> -->
                    <button name="make_available" string="Make Available" type="object" class="btn-primary"/>
                    <button name="make_borrowed" string="Make Borrowed" type="object" class="btn-primary" />
                    <button name="make_lost" string="Make Lost" type="object" class="btn-primary"/>
                    <!-- <button name="average_book_occupation" string="Log Average Occ." type="object" class="btn-primary"/> -->
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="author_ids" widget="many2many_tags"/>
                            <field name="category_id"/>
                            <field name="cost_price"/>
                            <field name="state"/>
                            <field name="pages"/>
                            <field name="notes"/>
                            <!-- <field name="old_edition" /> -->
                        </group>
                        <group>
                            <field name="active"/>
                            <field name="short_name"/>
                            <field name="date_release"/>
                            <field name="date_updated"/>
                            <field name="cover" widget="image" class="oe_avatar"/>
                            <field name="reader_rating"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Tree(list) View -->
    <record id="library_book_view_tree" model="ir.ui.view">
        <field name="name">Library Book List</field>
        <field name="model">library.book</field>
        <field name="arch" type="xml">
            <tree>
                <!-- <field name="cover" widget="image" class="oe_avatar" size="2"/> -->
                <field name="name"/>
                <field name="date_release"/>
                <field name="author_ids" widget="many2many_tags"/>
                <field name="state"/>
            </tree>
        </field>
    </record>

    <!-- Search View -->
    <record id="library_book_view_search" model="ir.ui.view">
        <field name="name">Library Book Search</field>
        <field name="model">library.book</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="author_ids"/>
                <filter string="Without Authors" name="without_author" domain="[('author_ids','=',False)]"/>
            </search>
        </field>
    </record>

</odoo>